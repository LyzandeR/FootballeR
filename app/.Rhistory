print('yo')
library(shiny)
shiny.launch.browser
getOption('shiny.launch.browser')
?cbind
methods('cbind')
footy_tab <- setDT(read.csv('C:/Users/teoboot/Downloads/E0.csv', stringsAsFactors=FALSE))
footy_tab[, Date := as.IDate(Date, format='%d/%m/%y')]
library(data.table)
library(highcharter)
library(DT)
footy_tab <- setDT(read.csv('C:/Users/teoboot/Downloads/E0.csv', stringsAsFactors=FALSE))
footy_tab[, Date := as.IDate(Date, format='%d/%m/%y')]
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team            = x,
`Games Played`  = .N,
Points          = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`     = sum(ifelse(FTR == 'H', 1, 0)),
Draws           = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`   = sum(ifelse(FTR == 'A', 1, 0)),
`Goals Scored`  = sum(FTHG),
`Goals Conceded`= sum(FTAG)),
by='HomeTeam']
})
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team            = x,
`Games Played`  = .N,
Points          = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`     = sum(ifelse(FTR == 'H', 1, 0)),
Draws           = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`   = sum(ifelse(FTR == 'A', 1, 0)),
`Goals Scored`  = sum(FTHG),
`Goals Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team            = x,
`Games Played`  = .N,
Points          = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`     = sum(ifelse(FTR == 'A', 1, 0)),
Draws           = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`   = sum(ifelse(FTR == 'H', 1, 0)),
`Goals Scored`  = sum(FTHG),
`Goals Conceded`= sum(FTAG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
cbind(home_team_stats, away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team, Played,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`)]
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played               = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played               = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTHG),
`Away Conceded`= sum(FTAG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team, Played,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`)]
team_stats
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played               = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played               = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTHG),
`Away Conceded`= sum(FTAG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team, Played,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`, Points1,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`, Points2)]
team_stats[, `Goal Diff` := `Home Scored` + `Away Scored`][, Points := Points1 + Points2]
team_stats
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played               = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played               = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTHG),
`Away Conceded`= sum(FTAG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team, Played,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
Points = Points1 + Points2)][, `Goal Diff` := `Home Scored` + `Away Scored`]
team_stats
team_stats
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played               = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played               = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTHG),
`Away Conceded`= sum(FTAG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team, Played,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` := `Home Scored` + `Away Scored`,
Points = Points1 + Points2)]
team_stats
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played               = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played               = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTHG),
`Away Conceded`= sum(FTAG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team, Played,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` = `Home Scored` + `Away Scored`,
Points      = Points1 + Points2)]
team_stats
?setorder
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played1              = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played2              = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTAG),
`Away Conceded`= sum(FTHG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team,
Played = Played1 + Played2,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` = `Home Scored` + `Away Scored` - `Home Conceded` - `Away Conceded`,
Points      = Points1 + Points2)]
setorder(team_stats, -Points, -`Goal Diff`, -(`Home Scored` + `Away Scored`))
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played1              = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played2              = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTAG),
`Away Conceded`= sum(FTHG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team,
Played = Played1 + Played2,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` = `Home Scored` + `Away Scored` - `Home Conceded` - `Away Conceded`,
Points      = Points1 + Points2)][, total_scored := `Home Scored` + `Away Scored`]
setorder(team_stats, -Points, -`Goal Diff`, -total_scored)
team_stats[, Position = .I]
team_stats[, total_scored := NULL]
team_stats
team_stats[, Position := .I]
team_stats
team_stats[, list(Position, names(team_stas)[length(names(team_stats))])]
team_stats[, list('Position', names(team_stats)[length(names(team_stats))]), with = FALSE]
team_stats[, c('Position', names(team_stats)[length(names(team_stats))]), with = FALSE]
team_stats[, c('Position', names(team_stats)[-length(names(team_stats))]), with = FALSE]
setwd('c:/github/Footballer/app/')
runApp()
runApp()
options(browser = "C:/Program Files (x86)/Google/Chrome/Application/chrome.exe")
runApp()
runApp()
?table
sketch <- htmltools::withTags(table(
class = 'display',
thead(
tr(
th(colspan = 3, ''),
th(colspan = 5, 'HOME'),
th(colspan = 5, 'AWAY')
),
tr(
lapply(names(team_stats), th)
)
)
))
sketch
runApp()
sketch <- htmltools::withTags(table(
id = 'display',
thead(
tr(
th(colspan = 3, ''),
th(colspan = 5, 'HOME'),
th(colspan = 5, 'AWAY')
),
tr(
lapply(names(team_stats), th)
)
)
))
sketch
runApp()
runApp()
runApp()
runApp()
renderDataTable()
renderDataTable
?datatable
runApp()
runApp()
?renderTable
renderTable({
footy_tab  <- values$data
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played1              = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played2              = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTAG),
`Away Conceded`= sum(FTHG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team,
Played = Played1 + Played2,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` = `Home Scored` + `Away Scored` - `Home Conceded` - `Away Conceded`,
Points      = Points1 + Points2)][, total_scored := `Home Scored` + `Away Scored`]
setorder(team_stats, -Points, -`Goal Diff`, -total_scored)
team_stats[, Position := .I]
team_stats[, total_scored := NULL]
team_stats <- team_stats[, c('Position', names(team_stats)[-length(names(team_stats))]), with = FALSE]
# #make the custom container before returning the datatable
# sketch <- htmltools::withTags(table(
#   class = 'display',
#   thead(
#     tr(
#       th(colspan = 3, ''),
#       th(colspan = 5, 'HOME'),
#       th(colspan = 5, 'AWAY')
#     ),
#     tr(
#       lapply(names(team_stats), th)
#     )
#   )
# ))
team_stats
})
?renderTable
runApp()
runApp()
?tableOutput
?renderTable()
team_stats
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played1              = .N,
Points1              = sum(ifelse(FTR == 'H', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1, 0)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1, 0)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played2              = .N,
Points2              = sum(ifelse(FTR == 'A', 1, 0)) * 3 + sum(ifelse(FTR == 'D', 1, 0)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1, 0)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1, 0)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1, 0)),
`Away Scored`  = sum(FTAG),
`Away Conceded`= sum(FTHG)),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team,
Played = Played1 + Played2,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` = `Home Scored` + `Away Scored` - `Home Conceded` - `Away Conceded`,
Points      = Points1 + Points2)][, total_scored := `Home Scored` + `Away Scored`]
setorder(team_stats, -Points, -`Goal Diff`, -total_scored)
team_stats[, Position := .I]
team_stats[, total_scored := NULL]
team_stats <- team_stats[, c('Position', names(team_stats)[-length(names(team_stats))]), with = FALSE]
team_stats
runApp()
runApp()
runApp()
runApp()
?xtable
renderTable({
footy_tab  <- values$data
teams <- unique(c(footy_tab[ , unique(HomeTeam)], footy_tab[ , unique(AwayTeam)]))
home_team_stats <- lapply(teams, function(x) {
footy_tab[HomeTeam==x, list(Team                 = x,
Played1              = .N,
Points1              = sum(ifelse(FTR == 'H', 1L, 0L)) * 3 + sum(ifelse(FTR == 'D', 1L, 0L)),
`Home Wins`          = sum(ifelse(FTR == 'H', 1L, 0L)),
`Home Draws`         = sum(ifelse(FTR == 'D', 1L, 0L)),
`Home Losses`        = sum(ifelse(FTR == 'A', 1L, 0L)),
`Home Scored`  = sum(FTHG),
`Home Conceded`= sum(FTAG)),
by='HomeTeam']
})
away_team_stats <- lapply(teams, function(x) {
footy_tab[AwayTeam==x, list(Team                 = x,
Played2              = .N,
Points2              = sum(ifelse(FTR == 'A', 1L, 0L)) * 3 + sum(ifelse(FTR == 'D', 1L, 0L)),
`Away Wins`          = sum(ifelse(FTR == 'A', 1L, 0L)),
`Away Draws`         = sum(ifelse(FTR == 'D', 1L, 0L)),
`Away Losses`        = sum(ifelse(FTR == 'H', 1L, 0L)),
`Away Scored`  = as.integer(sum(FTAG)),
`Away Conceded`= as.integer(sum(FTHG))),
by='AwayTeam']
})
home_team_stats <- rbindlist(home_team_stats)
away_team_stats <- rbindlist(away_team_stats)
team_stats <- cbind(home_team_stats, away_team_stats)[, list(Team,
Played = Played1 + Played2,
`Home Wins`, `Home Draws`, `Home Losses`, `Home Scored`, `Home Conceded`,
`Away Wins`, `Away Draws`, `Away Losses`, `Away Scored`, `Away Conceded`,
`Goal Diff` = `Home Scored` + `Away Scored` - `Home Conceded` - `Away Conceded`,
Points      = as.integer(Points1 + Points2))][,
total_scored := `Home Scored` + `Away Scored`]
setorder(team_stats, -Points, -`Goal Diff`, -total_scored)
team_stats[, Position := as.character(.I)]
team_stats[, total_scored := NULL]
team_stats <- team_stats[, c('Position', names(team_stats)[-length(names(team_stats))]), with = FALSE]
team_stats
},include.rownames = FALSE)
gvisTable
install.packages('googleVis')
?gvisTable
library(googleVis)
?gvisTable
tbl1 <- gvisTable(Population)
plot(tbl1)
?gvisTable
